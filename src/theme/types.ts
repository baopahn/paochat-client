export interface ThemeType {
  isDark: boolean;
  primary: string;
  secondary: string;
  text: string;
  background: string;
  button: string;
  border: string;
  icon: string;
}

export default ThemeType;
