export interface ThemeType {
  isDark: boolean;
  primary: string;
  secondary: string;
  text: string;
  background: string;
  button: string;
  border: string;
  icon: string;
  card: string;
  zIndexModal: number;
  overlay: string;
}

export default ThemeType;
